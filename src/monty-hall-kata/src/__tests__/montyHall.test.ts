import { switchDoor, stayDoor } from '../montyHall';

describe('Monty Hall Problem', () => {
  // test('Player switches doors and wins the car', () => {
  //   const result = switchDoor(['goat', 'car', 'goat'], 0, 2); // Player switches from door 0 to remaining door
  //   expect(result).toBe('car');
  // });

  // test('Player stays with original door and wins the goat', () => {
  //   const result = stayDoor(['goat', 'car', 'goat'], 0); // Player stays at door 0
  //   expect(result).toBe('goat');
  // });
});
